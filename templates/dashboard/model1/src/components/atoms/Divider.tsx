/**
 * Renders a full-width horizontal divider.
 * This component can be used to visually separate content within a layout.
 */
export default function Divider() {
  return (
    <div className="w-full h-[1px] bg-border"></div>
  )
}