type Testimonial = {
  logoImg: string;
  title: string;
  enterprise?: string;
  description?: JSX.Element;
  authorName: string;
  authorImg: string;
  authorPosition: JSX.Element;
};

export type { Testimonial };
